# CONTEXT · 长视频浓缩工作流

术语表。只收录领域语言，不含实现细节。

## 物料侧

- **素材（Material）**：一条输入视频 + 其配套文稿，以 video_id 标识。video_id 不编码业务含义。当前业务约束：**纯剧情实录，无第三方口播**（UP 主解说、实况主声音不存在于素材中）。
- **分支差异**：已确认可忽略——分支仅造成秒级错位，不存在大段差异。对齐按单向容错处理即可，无需分支感知。
- **台词行（Voiced Line）**：文稿中有对应配音语音的一行对话。对齐的基本单位。
- **选项文本（Choice Text）**：文稿中由玩家选择触发的文本，**无配音**，不得参与对齐。（来源实证：BWIKI `{{剧情选项|选项N=…}}`）
- **分支台词（Branch Lines）**：因玩家选择而互斥的多组台词行。文稿全收，视频中仅一组实际出现，未出现的组在对齐中自然落空。（已确认：实际业务中分支差异仅秒级，按普通未匹配行处理）

## 结构侧

- **镜头（Shot）**：场景检测切出的最小画面单元，只作为元数据存在，不做物理切分。
- **场景（Scene）**：相邻镜头归并出的语义单元。层级：镜头 → 场景 → 全片。
- **任务（Task）**：一部成片的生产单位。引用一个或一组素材（按 seq 有序），多素材在任务全局时间轴上合流。
- **画面分级（Shot Class）**：镜头的画面质量分级，E（最高）→ A（最低），按内容类型可配置，属类型适配层。分类信号中的 **UI 含常驻界面元素与游戏对话框**（故纯对话镜头自然落入 A/B）。
- **字幕模式（subtitle_mode）**：系列级配置。`letterbox`=上下裁切电影画幅、字幕烧在黑边上；`overlay`=白边描边字幕压住原画面（默认）。

## 生产侧

- **时间轴索引（Timeline）**：每素材一份的结构化事实源：镜头 × 台词行 × 画面分级互相关联，均带时间戳。
- **解说稿（Narration）**：LLM 生成的解说文本，每句引用时间区间。
- **EDL（剪辑决策清单）**：片段的有序列表，每片段记录 `(video_id, 源内时间区间)` 与类型；**不含成片绝对时间**，成片时间轴渲染时累积求和（相对时间轴铁律）。
- **raw_insert（原声高光段）**：EDL 中保留原始画面与原始音频的人工指定片段。播放时解说让位（该区间不排解说句），BGM 自动压低。与解说片段时间不得重叠。
- **闸口（Gate）**：流程中必须人工确认才能继续的节点。闸口1=解说稿评审，闸口2=选片评审。
- **片段使用登记（footage_usage）**：台账中的一本账，按**镜头级**记录已进入成片的画面。同一源镜头在所有成片中只许出现一次（片头素材豁免）；选片时排除已占用镜头。
- **类型适配层（Series Profile）**：按内容系列配置的一组规则：画面分级表、命名模板、片头组合、画面变换参数。
