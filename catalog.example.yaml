# 登记示例（进 Git；真实登记走 `mmm add-asset`，不再维护 YAML 台账）
# 旧 catalog.yaml / plan.csv 已废除，统一走 DATA_ROOT/ledger.sqlite。

# 视频分P（seg = quest 内剧情序号，1 起；非 bilibili 分P号）
# mmm add-asset --game genshin --version 1.6 --slug midsummer-islands \
#     --kind video --seg 1 --src /本地/p001.mp4 \
#     --type event --quest-name "盛夏！海岛？大冒险！"

# 台词（每 quest 一份 dialog）
# mmm add-asset --game genshin --version 1.6 --slug midsummer-islands \
#     --kind dialog --src /本地/台词.jsonl

# 版本 BGM（版本级，一版本多首）
# mmm add-asset --game genshin --version 1.6 --slug midsummer-islands \
#     --kind bgm --src /本地/ost候选.mp3

# 认领（管道与独立模式共用）
# mmm task-create --game genshin --version 1.6 --slug midsummer-islands --mode narrate
